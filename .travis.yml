sudo: false
language:
    - java
install:
  - mvn --version
script: 
  - travis_wait mvn -T4 install -DskipTests=true -Dmaven.javadoc.skip=true
jdk:
  - oraclejdk8
env:
  - MAVEN_OPTS="-Xms2048m -Xmx=2048m"
branches:
  only:
    - travis-ci-build

